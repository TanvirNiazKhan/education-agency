[phases.setup]
nixPkgs = ["nodejs_20", "corepack"]

[phases.install]
cmds = ["corepack enable && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm turbo build --filter=@meridian/admin"]

[start]
cmd = "pnpm --filter @meridian/admin start"
